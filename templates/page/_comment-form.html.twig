{% if app.user %}
    <p>
        Comment as <strong>{{ app.user.name }}</strong>
    </p>
    {{ form(form, { 'action': path('app_comment_create', { slug: post.slug }) }) }}
{% else %}
    <a href="{{ path('app_login') }}">Login</a>
{% endif %}